import { SpinnerContainer } from "../styles/fallbackStyle"

const Fallback=()=>{
    return <SpinnerContainer>
        <div className="nfLoader"></div>
    </SpinnerContainer>
}
export default Fallback